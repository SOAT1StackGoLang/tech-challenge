version: '3.8'

services:
  db:
    image: postgres
    restart: always
    volumes:
      - ./01-base.sql:/docker-entrypoint-initdb.d/01-base.sql
      - ./02-carga.sql:/docker-entrypoint-initdb.d/02-carga.sql
    environment:
      POSTGRES_PASSWORD: "teste123"
    ports:
      - 5432:5432